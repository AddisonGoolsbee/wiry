# Deviations, cut corners, and known gaps

Running log of every simplification made against full Scapy parity. Nothing here is
hidden from users: the README links this file, and the launch benchmark will state
the supported surface explicitly.

Status key: **OPEN** = still a gap · **CLOSED** = resolved, kept for history.

---

## Scope decisions (deliberate, made 2026-09-15)

| # | Decision | Rationale | Status |
|---|---|---|---|
| S1 | Protocol scope was Ether, Loopback (DLT_NULL/DLT_LOOP), CookedLinux (LINUX_SLL, LINUX_SLL2), ARP, IPv4, IPv6, TCP, UDP, ICMP, ICMPv6, DNS, DHCP/BOOTP, VLAN(802.1Q), Raw, Padding; the encapsulations of E6 (the four IPv6 extension headers, GRE, VXLAN, Geneve, MPLS, PPPoE/PPP, GTP-U, ERSPAN) and sixty more layers from `dev/protogen/specs/` were added, at the depth the **Protocol coverage** table below states one row at a time | Scapy registers 1,746 layers on import and 4,160 `Packet` subclasses with contrib. Full parity is multi-person-year. This set covers the overwhelming majority of real scripts. | OPEN |
| S2 | Live capture and injection are being added behind the `live` feature, off by default. `sniff(offline=...)` is complete and needs no feature, no privileges and no network; `sniff(iface=...)`, `AsyncSniffer`, `send`, `sendp`, `sr`, `sr1`, `srp` and `srp1` are implemented against the same state machine and need the feature; without it every one raises `CaptureUnavailable` (a subclass of `OSError`) naming the rebuild command | The capture backend needs raw sockets, root, and per-OS libraries, so it stays untestable in ordinary CI and out of the default build. Driving the whole state machine from `offline=` is what makes it testable at all: counters, deadline, the three filter stages and the callback contract are proved with zero privileges, and the live backend becomes an I/O shim over proven logic. Privileged round-trip checks live in `dev/live/`: `netns_check.py` over a veth pair on Linux, and `loopback_check.py` on macOS, where `lo0` is DLT_NULL rather than Ethernet. | PARTIAL |
| S3 | No Scapy import at runtime. **Revised 2026-09-18**: the "clean-room" half of this decision is withdrawn — wiry is now a derivative work of Scapy, licensed GPL-2.0-only, and copies from it deliberately (`NOTICE`, `CONTRIBUTING.md`, `CLAUDE.md` §2). The no-runtime-import half stands, unchanged and for a different reason | Two reasons used to be one. **The licensing reason is gone**: under GPL-2.0 importing Scapy would be permitted, and the reach it was protecting — proprietary adopters and the permissively-licensed crates.io ecosystem — was traded away with the relicence, so it no longer argues for anything. **The architecture reason is what remains, and it was always the stronger one**: Scapy builds one Python object per layer per packet, which is the cost this engine exists to remove, so a fallback that imported Scapy for unimplemented protocols would reintroduce it at exactly the layers a user could not avoid, and would put a Python object back into the bulk paths CLAUDE.md §3 keeps object-free. It would also make the dependency's absence unobservable until a capture happened to contain the protocol. Unknown protocols dissect to `Raw` instead (S4), which is what Scapy itself does for layers it lacks. Copying Scapy's *source* into wiry's tree at build time is a licensing question, now answered yes; *importing* Scapy at run time is an engine question, still answered no | CLOSED |
| S4 | Unimplemented protocols dissect to `Raw`, exactly as Scapy does for layers it lacks | Preserves correct round-trip bytes for everything, at any depth. | CLOSED |
| S5 | Name `wiry` is registered on PyPI, crates.io and GitHub (`github.com/AddisonGoolsbee/wiry`), and is no longer provisional | Deliberately not squatted until the project was real. | CLOSED |

## Provenance policy (licensing-critical)

**Reversed 2026-09-18.** wiry is a derivative work of Scapy and is licensed
GPL-2.0-only. Copying from `scapy/layers/*.py` is permitted and is the plan; what GPL
requires instead is that the derivative stay GPL-2.0-only, that Scapy's copyright
notices be preserved, and that files carrying derived material state that they were
changed and when. `NOTICE` holds the attribution, the per-file block format and the
ledger; `CONTRIBUTING.md` is the procedure; `CLAUDE.md` §2 records why the position
changed and what it cost. The *Google v. Oracle* fair-use argument is no longer what
the licence rests on.

Two habits survive the reversal, neither of them a licensing control any more. RFC and
IANA citations are still required (`dev/protogen/`'s `citation` key still fails the
build when missing) because a specification is a better source of truth than any
implementation. Golden test vectors are still hand-built from RFC examples and
Wireshark/tshark rather than generated by running Scapy, because a suite whose
expectations came out of Scapy cannot detect that wiry and Scapy are both wrong — a
sharper risk now that wiry copies from Scapy, not a duller one.

Commits through `f756662` were published under MIT and remain MIT for anyone who took
them. Relicensing is not retroactive; new work from the relicence commit forward is
GPL-2.0-only.

## Engineering shortcuts

| # | Shortcut | Consequence | Status |
|---|---|---|---|
| E1 | Field model is a static table per protocol, but a field may now carry a condition over its own layer's header bytes (`FieldDesc::cond`). ICMP uses it for the whole RFC 792/950/1191/4884 type-dependent layout: `id`/`seq`, `gw`, `ptr`, `reserved`/`length`/`nexthopmtu`, the three timestamps and `addr_mask` are each present only for the types that define them, the header grows to 12 or 20 octets where the type requires it, and a read, write or construction of a field the message does not carry is refused rather than aliasing another field's octets. Two fields may share a name when their conditions are disjoint, which covers `MultipleTypeField` | A layer may also carry *framing* contributed by the pair it sits in rather than by either header alone: `proto::framing_octets(parent, child)` gives DNS its RFC 1035 §4.2.2 stream length prefix, and the dissector counts those octets into the header ahead of every field. Since that moves every field, the framed layout is a second static table (`proto::fields_of(id, framed)`), not a second condition | Remaining: a *condition* still sees only its own layer's header, so a field whose presence depends on the enclosing layer is expressible only where that layer also changes the offsets, as framing does. `ICMP.ext`/`extpad` (RFC 4884 §7) live past the quoted datagram, in what this model calls the payload, so they exist as names only. No arbitrary computed fields, and no conditions definable from Python (see E3) | PARTIAL |
| E2 | IPv4/TCP/DHCP options are read and written: `pkt[TCP].options` returns a named list, and `TCP(options=[('MSS', 1460)])` encodes one. Raw bytes accepted too, and `raw_options()` returns them. A parsed list feeds straight back to the constructor: an option the parser has no name for is labelled with its code in decimal, and that label encodes as that code again, so parse-then-rebuild is byte-exact for DHCP option 60 and every other unnamed code. Writing the field on an *already dissected* packet resizes the option region and moves `ihl`/`dataofs` with it, rather than writing over whatever followed; a region wider than those four-bit fields can describe (40 octets for both IPv4 and TCP) is refused instead of wrapping. TCP SACK blocks (RFC 2018 §3) are split into a list of `(left, right)` edge pairs, and a list of pairs — or a flat list of edges — encodes back to the same octets; a SACK payload that is not a whole number of blocks keeps its raw octets rather than being silently re-cut. Bulk (`field_column`, `columns`) and per-packet reads return the same parsed list, and both now go through one `packet::options_at`, so the DHCP overload and joining of E7 cannot reach only one of them. A bulk column or filter condition resolves its field against *both* layouts a layer can take (E1 framing) before the loop starts, so a framed layer is never read through the unframed table's offsets and an unframed one never answers to a name only the framed table declares | Remaining: a derived field the caller pinned stays pinned, so scapy's habit of keeping a wire-read `dataofs` while adding options is not reproduced: wiry recomputes unless you set it, and scapy's own output matches wiry's once its derived fields are cleared | PARTIAL |
| E3 | Layers are definable from Python. Subclassing `Packet` with a `fields_desc` registers the layer in Rust at class-definition time; `wiry.fields` supplies `ByteField`, `XByteField`, `ShortField`, `XShortField`, `LEShortField`, `IntField`, `XIntField`, `LEIntField`, `LongField`, `XLongField`, `LELongField`, `BitField`, `IPField`, `IP6Field`, `MACField`, `StrFixedLenField`, `StrField` and `FlagsField`. `bind_layers(UDP, MyProto, dport=9999)` makes dissection reach it and makes stacking write the parent's field back. From then on it behaves as a generated built-in: `/` stacking, `pkt[MyProto]`, `in`, field get/set, `show()`, `summary()`, `bytes()`, dissection from a capture, and `columns()`/`filter()`. The declaration is data, never a callback: no Python runs during dissection | Remaining: a declared layer is a flat fixed-width table plus at most one trailing variable-length `StrField`, refused at class-definition time where the `StrField` is not last, so it has no conditional fields (E1), no computed lengths or checksums, and no option region (E2); `X*` fields are plain integers rather than hex-rendered; a binding tests equality on integer fields only, and it outranks the parent's own built-in guess where both match; layer names are global to the process and a built-in name cannot be taken over; registration is append-only and leaks, capped at 1,024 layers and 1,024 bindings; a layer's bit offsets are sixteen bits, so a `StrFixedLenField` is at most 8,191 octets wide, refused at declaration rather than after padding a default out to the width asked for; a `FlagsField` naming more flags than its width has bits is refused too, since those bits exist nowhere | PARTIAL |
| E4 | `show()` output formatting matched by eye, not byte-for-byte against Scapy | Cosmetic divergence possible in edge cases | OPEN |
| E5 | pcapng **reading and writing** implemented (`pcapng.rs`). Reading: SHB, IDB with `if_tsresol`, EPB, SPB, both endiannesses, unknown blocks skipped, trailing-length validated. Writing: SHB, IDB and EPB, each padded to a 4-byte boundary and closed with the trailing total length the reader validates, with `if_tsresol` declared when `nano=True`. `wrpcapng()`, `PcapNgWriter`, and any writer whose file is named `.pcapng` or `.ntar`, produce it | Modern captures read fine and yield the same `pcap::Record` as classic pcap, so callers need not care which format they got. A packet block naming an interface no IDB described is skipped, since nothing fixes its link type. What we write reads back identically through wiry's own reader, scapy 2.7.0 and dpkt, over every combination of format, resolution and compression (`dev/write_check.py`); tshark was not on the PATH when that was last run, so its half of the check has never been exercised. Still missing on the **write** side: every block but SHB, IDB and EPB — no Simple Packet Blocks, no Name Resolution Blocks, no Decryption Secrets Blocks, no per-packet options (so scapy's `pkt.comment` is dropped rather than written), one interface per section, and little-endian output only. Appending opens a *new* section rather than reusing an earlier IDB, since a packet block may only name an interface described in its own section. Still missing on the **read** side: per-interface link types when one file mixes them (`FileHeader.linktype` reports the first IDB's, though each interface's own `if_tsresol` is honoured per packet); multi-section files with differing link types (endianness and the interface table do reset at a new SHB, the reported link type does not); Name Resolution Blocks; Decryption Secrets Blocks; per-packet options. Resolutions other than 10^-6 and 10^-9 truncate to microseconds, since `Record.ts_frac` has no finer unit | PARTIAL |
| E6 | Encapsulation is walked rather than stopped at. The previous wording here — "no IPv6 extension-header chain walking beyond the common set", "exotic chains fall back to `Raw`" — understated the gap twice over: **no** extension header was walked, so hop-by-hop options and destination options (both ordinary), Router Alert, MLD and every fragmented IPv6 flow were invisible, and losing a tunnel loses everything under it, not one leaf protocol. Now: the four RFC 8200 §4 extension headers (Hop-by-Hop 0, Routing 43, Fragment 44, Destination Options 60) are layers of their own that chain into each other and out to the real transport; GRE (RFC 2784 with RFC 2890 key and sequence); VXLAN (RFC 7348); Geneve (RFC 8926); IP-in-IP and 6in4/4in6 (RFC 2003, RFC 4213, protocols 4 and 41 over both versions); MPLS (RFC 3032) with the label stack walked to its bottom; PPPoE Discovery and Session with PPP (RFC 2516, RFC 1661); GTP-U (3GPP TS 29.281) including its extension-header chain; ERSPAN Type I, II and III (draft-foschiano-erspan-03). They nest: the total depth is bounded at 32 layers by `packet.rs`, each header advances the offset so no chain can be self-referential, and a Fragment header past offset 0 carries no transport header, as IPv4 already did. `columns()` and `filter()` see inner layers, and the bulk path is asserted to agree with the per-packet one | Remaining: **MPLS and GTP-U have no protocol field**, so what follows is read from the payload's first nibble — 4 or 6 for an IP version (RFC 4385 §3 is what makes that a signal rather than a guess), and for MPLS anything else as an Ethernet pseudowire (RFC 4448 §4.6). That last arm is a guess and can be wrong the other way too: an Ethernet pseudowire with no control word whose destination MAC begins `4x` or `6x` reads as an IP datagram. Nothing in the packet can tell the two apart, so the ambiguity is the protocol's, not this implementation's, and it is why a control word (nibble 0) stays `Raw`. A leading zero nibble is a pseudowire control word, which has no layer here, so that payload stays `Raw` rather than being read as a frame shifted by four octets; scapy has an `EoMCW` layer and reads it, which is why one scapy regression test skips here instead of passing. GRE version 1 (PPTP, RFC 2637) is read with the version-0 layout, so its payload length and call id appear as `key` and its acknowledgement number is unread; the RFC 1701 source-route list is not walked, only its two-octet checksum/offset pair accounted for. `IPv6ExtHdrRouting` names its fields after Type 0, the only layout RFC 8200 §4.4 gives, so for a Type 4 (segment routing) header everything past `segleft` is `reserved`+`addresses` by name only. The option regions of Hop-by-Hop and Destination Options parse and encode (Pad1, PadN, RouterAlert, JumboPayload, HAO, PDM) but there is no automatic PadN insertion, so a block written by hand must land the header on an 8-octet boundary — scapy's `autopad` pseudo-field has no equivalent and `len` is recomputed from the span, skipped rather than rounded when it does not fill whole units; Geneve's `optionlen` works the same way over 4-octet units, and its option TLVs are opaque bytes. A region *wider* than the count can name is refused rather than skipped, since 256 octets of Geneve options would otherwise write 64 into six bits as 0 and swallow the block, and 2,054 octets of IPv6 options would clamp `len` to 255 and read the eight octets past the claim as the transport header; `PPPoE.len` and `GTP_U_Header.length` are refused past 65,535 for the same reason, where nothing encloses them that would catch the overflow first. A clipped capture keeps every one of these as the wire gave it. ERSPAN Type I has no layer object, because it has no header: GRE 0x88BE with the sequence bit clear stacks `Ether` directly. Stacking `ERSPAN_II` sets GRE's sequence bit and the word that comes with it, since without them the frame it built dissected back as a Type I with the mirrored frame eight octets out of place. ERSPAN Type III's platform-specific subheader is one opaque `platform` field rather than scapy's separate `ERSPAN_PlatformSpecific` layer. VXLAN-GPE, L2TP, Teredo, SRv6 and NSH are not implemented at all, and IPsec is a header rather than a tunnel: `ESP` and `AH` dissect from IP protocols 50 and 51 but nothing walks past them, because the payload is encrypted (P7) | PARTIAL |
| E7 | DHCP options (RFC 2132) are parsed into a named item list via `Packet::options`; the raw `options` field still exposes the untouched bytes. The magic cookie belongs to BOOTP's trailing `options` field, where RFC 2131 §3 puts it, so the DHCP layer starts at the first option and has `options` as its only field. Option 82 relay agent information (RFC 3046) is a nested option region of its own, walked by `bootp::RELAY_OPTIONS` and named (`agent_circuit_id`, `agent_remote_id`, `subscriber_id`, `link_selection`, `vendor_specific`, `relay_agent_flags`, `server_id_override`); the parsed sub-list encodes straight back. RFC 3396 concatenation joins a value split over repeated appearances of one code, joining the *octets* before anything decodes them, which is the only order that can rebuild a value whose halves do not each decode alone — half an address list, or half a nested region; Pad and End are left alone, which repeat by design. RFC 2131 §4.1 overload (option 52, now named) is honoured: `file` and then `sname` are walked as further option regions, in the order RFC 3396 §4 fixes | Remaining: joining means a message that split a value is rebuilt joined rather than split, so parse-then-rebuild is byte-exact only where the sender did not split — `wrpcap(rdpcap(f))` is unaffected either way, since it re-emits the buffer rather than a re-encoded option list; only the *first* option 52 selects the overload regions, so an option 52 inside `sname` or `file` is read as an ordinary option and never re-enters the walk, though its octets do join the outer one's under RFC 3396 and the value then reads as nonsense; the overload regions are read but never written, so `DHCP(options=...)` never overflows into `sname`/`file`; sub-option codes wiry does not name stay raw octets under their decimal label, as unnamed top-level options do | CLOSED |
| E8 | DNS record sections are parsed: `dns::parse_records` returns `qd`/`an`/`ns`/`ar` over the whole message, including name compression (RFC 1035 §4.1.4) with bounded pointer following, and decodes RDATA for A, NS, CNAME, SOA, PTR, MX, TXT, AAAA, SRV (RFC 2782), CAA (RFC 8659), OPT (RFC 6891) and the DNSSEC types DS, RRSIG, NSEC, NSEC3, NSEC3PARAM and DNSKEY (RFC 4034, RFC 5155), type bit maps included. An OPT pseudo-record also reports the fields RFC 6891 §6.1.3 puts in CLASS and TTL — UDP payload size, extended rcode, version and the DO bit — alongside its option list, each option named from the IANA EDNS0 registry. DNS over TCP works: `tcp.rs` dispatches port 53 and RFC 1035 §4.2.2's two-octet length prefix is a real `length` field that recomputes on write, with the twelve-octet header and the compression origin both shifted past it (see E1 framing). Names render in RFC 1035 §5.1 presentation form: a trailing root dot, and a dot or backslash inside a label escaped (RFC 4343 §2.1) | Remaining: the layer's payload still dissects as `Raw`, so bytes round-trip unchanged, and records are read but never written — there is no record encoder, which is also why a bare `DNS()` carries no question (E10). A TCP segment carrying more than one message dissects the first and leaves the rest in the payload; a write anywhere in such a packet therefore leaves the prefix alone rather than stretching it over the messages that follow, since a prefix already framing a shorter message describes bytes the layer does not own. `pkt[DNS].qd`/`an`/`ns`/`ar` are exposed from Python and listed by `fields()`, but they are parser-backed accessors (`proto::accessor_names`), not flat fields: they have no fixed offset or width and cannot be assigned. One message decodes at most 256 KB in total (`dns::MAX_DECODED_NAME_BYTES`) and returns what it read when that runs out, because a two-octet compression pointer yields up to 255 octets and a message claiming 65,535 records in every section otherwise amplified 2.75 MB of input into 568 MB of output. The budget charges every owned byte a record decodes to, not only names: a TXT character-string becomes an owned string, so RDATA of N zero octets yields N empty strings and uncharged that turned 17 MB of input into 656 MB; an NSEC type bit map turns 8,704 octets into 65,536 type numbers; an RRSIG signature, a DS digest and a DNSKEY public key are each bounded only by RDLENGTH. What the budget does *not* charge is the 112-octet `ResourceRecord` itself, so a section of minimal records — an empty name and no RDATA, eleven octets each — costs eleven input octets per record and about ten times that in memory. That factor is linear in the input, unlike the compression, TXT and bit-map cases the budget exists to break, and a walk cannot read more records than the input has room for, which `robustness.rs` asserts; charging the struct honestly would cap a message at ~2,300 records, below the ~5,900 a legitimate 64 KB stream message can hold, so the count is deliberately left bounded by the input length alone. No real message comes within a thousandth of the budget | PARTIAL |
| E9 | Source address defaults (`Ether.src`, `ARP.hwsrc`, `ARP.psrc`) stay zero at build time. `sendp`, `srp` and everything built on them fill the unset ones from the outgoing interface at *send* time, on a copy | Scapy queries the live interface while building, which makes `bytes(Ether())` depend on the host. Keeping the fill in the send path is what leaves construction reproducible and `dev/build_matrix.py` at 38/38 without becoming environment-dependent. The hardware address now comes from getifaddrs through the `mac_address` crate, so it is real on every platform a capture runs on rather than Linux only; sysfs remains the fallback for a Linux build with the `live` feature off, and `#![forbid(unsafe_code)]` is why the syscall is reached through a dependency rather than written here. `ARP.hwsrc` and `ARP.psrc` are filled too — `psrc` from the interface's first IPv4 address — which is what makes `arping` and `getmacbyip` work at all: a request carrying 00:00:00:00:00:00 and 0.0.0.0 gets answered by nobody. Remaining: an interface with no hardware address of its own (a loopback, a tunnel) still yields `None` and the field is left alone, which every caller treats as an ordinary outcome, and `get_if_hwaddr` raises `ValueError` there rather than returning a plausible-looking zero; `psrc` is left alone where `get_if_addr` finds no address, since filling it with `0.0.0.0` is no better than leaving it; only a packet still held as a field spec can be filled, so a dissected packet goes out as captured; byte output still differs from Scapy for these fields when built rather than sent | PARTIAL |
| E10 | Two more default-construction differences, both deliberate. A bare `DNS()` is the 12-byte header alone, where Scapy's ships a sample question record for `www.example.com`; writing a question needs the record encoder E8 still does not have, and inventing a hostname in a default is worse than an empty section. A bare `ICMP(type=13)` leaves the three timestamps zero, where Scapy fills them from the host clock, which is the E9 problem in another form: a default that changes between two runs of the same script | Every other layer is byte-identical to Scapy on default construction. `dev/api_audit.py` reports these two and ARP's interface MAC as the only remaining mismatches | OPEN |
| E11 | `flags` is a rich value, not a string. `pkt.flags` returns a `FlagValue` that compares equal to its integer and to its letter string, converts with `int()`/`str()`/`bool()`, hashes as its integer, exposes every bit as a boolean attribute where a concatenation of names is the AND of those bits (`.SA`), writes through to the packet when a bit is assigned, supports `&`, `|`, `^` and their in-place forms against an int, a string or another `FlagValue`, iterates its set names, and reprs as `<Flag 18 (SA)>`. Every `FieldKind::Flags` field has it, IPv4's and BOOTP's included. Names longer than a letter are joined with `+` (`MF+DF`), in the engine's renderer as well, so bulk columns and per-packet reads keep agreeing | Two bugs fell out of the work: an unassigned bit carries an empty name, which every string contains, so `BOOTP(flags="B")` used to set all sixteen; and TCP's control bits are nine wide, not eight, since NS (RFC 3540) takes the low bit of the reserved field. Bulk paths (`field_column`, `columns`) still return the plain string for a *flags* field, which costs one set lookup on the per-packet read path and no allocation; *option* fields now return the same parsed list from both. A name matching no flag raises rather than reading as zero, as scapy does, and matching is whole-token: substring matching meant that with names `a`, `ab` and `b` the wire value 2 read back as 7 | CLOSED |
| E12 | `Padding` is excluded from every enclosing length and checksum, and floats to the end during assembly: `IP()/Padding("abc")` has `len == 20` in a 23-byte frame, and `Raw/Padding/Raw/Padding` emits both `Raw` parts first. Dissection produces `Padding` too — IPv4, IPv6 and UDP declare the extent their length field claims, the walk stops there, and the trailer on a frame padded to Ethernet's 60-octet minimum becomes a `Padding` layer instead of transport data | Remaining: stacking `Padding` onto an already-dissected packet appends its bytes as payload rather than as a layer, because a materialised packet cannot be turned back into a field spec (same cause as the note in `__truediv__`). A capture clipped by the snaplen keeps the lengths the wire gave, since they describe bytes that were never captured; only the IPv4 header checksum, which covers present bytes, is still recomputed | PARTIAL |
| E13 | The conveniences scapy's suite leans on are implemented: `str` payloads (`Raw("abc")`, `pkt / "abc"`, `"abc" / pkt`, `add_payload`) encoded as latin-1, integer layer indexing (`pkt[0]`, `pkt[-1]`), `getlayer(IP, ttl=3)` with field filters and an occurrence count, the `pkt[IP::{"ttl": 3}]` slice form, `ls(pkt, verbose=True)`, iteration yielding one independent copy, and `copy()`. `wrpcap()` already accepted a mix of packet types | Address generators (`IP(dst=["a","b"])`, `IP(ttl=(5,10))`, `Net`) are implemented, in E21. Hostname resolution in an address field stays **out of scope and not planned**: resolving a name at build time would put DNS in the build path of every packet. `rdpcap` and `PcapReader` now accept a binary stream as well as a path, which is what lets scapy's OSS-Fuzz regression inputs run against us; the stream is written to a temporary file first, because the reader indexes records by offset into one mapped buffer. gzip input is accepted too, by magic bytes rather than by file name, on the same path and for both formats; the trailing CRC is read past rather than enforced, so a capture cut off mid-transfer still yields the records that arrived, and a file name ending `.gz` compresses the output. Decompression is bounded, because a decompressor is a walk over an attacker-controlled length: output is written a chunk at a time rather than accumulated, the first chunk has to carry a pcap or pcapng magic, and the total is capped at `_MAX_GUNZIP` (4 GiB), so a 200 KB bomb that used to expand to nearly a gigabyte of resident memory is now refused at its first chunk. Over the whole of scapy's `test/` directory `dev/scapy_suite.py` reports 59 pass, 616 skip and 5 fail, 47/294/4 over `regression.uts` alone, and 7/8/0 over `test/scapy/automaton.uts` (2026-09-18). Two harness corrections are in those figures: the capability probe walks a module `__dir__` that includes the lazily exported names, without which 25 tests skipped for a `conf` that exists, and a documented `NotImplementedError` counts as a scope boundary exactly as `CaptureUnavailable` already did. The five failures: two assert by patching a scapy internal we do not have (`scapy.utils.warning`, and `scapy.supersocket.select` — that second one, `Test SuperSocket.select`, appeared when E27 gave wiry a `SuperSocket` of its own, and no wiry behaviour can pass it, since the mock it installs is on scapy's module and never reaches ours); one needs a Windows host; and two are the generator divergences E21 states | PARTIAL |
| E14 | Reply matching for `sr`/`sr1` (`answers.rs`) is a closed table: ICMP and ICMPv6 echo, an ICMPv4 error (types 3, 4, 5, 11, 12) quoting the datagram that provoked it, a swapped TCP or UDP address and port pair, that plus a DNS **response** (QR set) carrying the id a DNS **query** asked with, and an ARP reply for the address asked about. Anything else is not an answer — there is deliberately no fallback on the address pair alone, since a wrong match silently corrupts a user's results where a missed one is visible in `unanswered`. Every rule pins the addresses down in both directions, the two echo rules and the quoted header included, because `sr` opens the handle promiscuously and an error or a reply meant for a third party is on the wire to be mismatched. An ICMP error is matched on the quoted header's source and destination, protocol and IP id, then the first 8 transport octets: the addresses come first because wiry builds every datagram with `id = 1` and a default `ICMP()` payload is the same eight octets every time, so a multi-target `sr()` would otherwise pair every reply with whichever probe came first. The TCP ack relation is computed by `ack_consistent` but never gates a match, so a RST answering a SYN still counts | Remaining: ICMPv6 errors (RFC 4443 types 1-4) quote an IPv6 datagram and are not matched, so an IPv6 traceroute has no reply rule; a reply is matched against one sent packet at a time, with no notion of a best match when two sent packets would both accept it; the quote is read only as far as RFC 792 guarantees it (the internet header plus 64 bits), so two probes differing only past that first payload octet are still indistinguishable. `sr`, `sr1`, `srp` and `srp1` are built on it: the receive capture opens before anything is sent, `retry=N` resends the unanswered set N more times, `multi=True` collects several answers per probe, and anything `answers()` does not match is dropped rather than guessed at, so it shows up in `unanswered`. `retry=` and `multi=` both need a `timeout=` and raise `ValueError` without one, since the first round otherwise waits until every probe is answered and neither a retry round nor a `multi` collection can ever end. Scapy's *negative* `retry` (resend only while nothing at all has answered) raises `NotImplementedError`; `answered` is a plain list of `(sent, received)` pairs and `unanswered` a plain list, not scapy's `SndRcvList`/`PacketList`, since the sent packets are never indexed into a capture buffer. `conf.checkIPaddr = False` selects `answers_matching(.., check_addr = false)`, which drops the address pinning and leaves the protocol identity — ports, echo id and seq, DNS id, the quoted header past its addresses — as the whole of the match. That is what a DHCP exchange needs, where the server answers from an address the client could not have predicted, and it is strictly looser everywhere else: the warning about silent mispairing applies with full force, which is why it is off by default. The pairing itself is exposed to Python as `_wiry.pair_replies`, the collect half of `sr` with the reads replaced by a list of frames — the same `Exchange` and the same `answers()`, no socket and no `live` feature — so a sweep's pairing is checked offline against canned replies rather than only against a router | PARTIAL |
| E15 | `sniff` is keyword-only and takes `iface`, `count`, `store`, `prn`, `filter`, `lfilter`, `timeout`, `stop_filter`, `offline`, `quiet`, `promisc`, `snaplen`, `session`, plus the wiry extension `where=`. `count=0` is unbounded, `store=0` returns an empty `PacketList`, and `timeout` is scapy's wall-clock deadline for the whole call, not libpcap's read timeout. Filters AND together cheapest first: `filter=` BPF, then `where=`, then `lfilter=`. `AsyncSniffer` has `start`, `stop`, `join`, `.results` and `.running`, and `stop()` is honoured between packets on the offline driver too | Remaining, all deliberate: scapy is positional-tolerant where this is keyword-only; `started_callback=`, `L2socket=` and `monitor=` are not accepted and raise `TypeError` rather than being silently ignored, and `opened_socket=` is accepted only to raise `NotImplementedError` naming the reason (E27); `session=` is E25's; `offline=` takes a path or a `PacketList`, and a plain list of packets raises `NotImplementedError` naming `wrpcap` rather than growing a second state machine that never touches a capture buffer; `quiet=True` suppresses the printing of `prn`'s return value, which is the only output `sniff` produces here; `count` counts packets that survived every filter, as scapy's does; a `timeout` no `Duration` can hold (infinity, `1e300`) is no deadline at all, i.e. exactly `timeout=None`, where it used to panic the Rust side, and a negative one still expires at once; a BPF `filter=` needs the `live` feature even offline, because compiling one is libpcap's job, and without it raises `CaptureUnavailable` saying so. `AsyncSniffer` always installs a stop check on the offline driver, so that one takes the per-packet slow path even with no user callback. Live, `sniff(iface=...)` installs `filter=` on the handle so BPF runs in the kernel, releases the GIL across every read and polls `count`, `timeout` and the stop flag between reads rather than per matched packet; a callback-free live sniff runs the loop in 250 ms slices with the GIL released, reacquiring only to check for a signal so that an unbounded `sniff` still answers Ctrl-C. A live `AsyncSniffer` is a Rust thread owning the handle: `stop()` sets a flag the loop polls between reads, `join()` waits with the GIL released, and dropping one stops and joins it, with an `atexit` hook over a `WeakSet` of live sniffers as a second line. Those waits run in the same 250 ms slices the capture loop does, with a signal check between them, so Ctrl-C interrupts a shutdown as well as a capture; a `Drop` cannot raise, so an interrupt that lands during one is handed back to whatever runs next. A `prn` given to a live `AsyncSniffer` therefore runs on a non-Python thread and reacquires the GIL per packet, which is scapy's contract for async callbacks — and a `prn` calling `stop()` or `join()` on its own sniffer gets `RuntimeError: cannot join current thread`, which is what `threading.Thread.join()` says and what the offline driver in the same class already raised; the capture is still stopped, only the wait is refused. `start`, `stop` and `join` are serialised by a lock the sniffer holds only across the state change and never across a wait, so two threads cannot both start one sniffer, and one run is all a sniffer gets: starting a finished one raises rather than silently discarding its `.results` | PARTIAL |
| E16 | `conf` carries the knobs scripts actually read, and every one of them does something. Real: `conf.iface` (resolved from libpcap on first read, settable); `conf.verb`; `conf.promisc`, the default for the `sr` family's `promisc=`; `conf.sniff_promisc`, the same for `sniff`; `conf.checkIPaddr`, which selects the address pinning in `answers.rs` (see E14); `conf.debug_dissector`, which makes a session's own failure raise rather than be reported and skipped past; `conf.recv_poll_rate`, how long a state machine's `select` waits before it looks at its timers; `conf.loopback_name`, which `route()` answers with and which defaults to what this host calls its loopback; `conf.route_autoload`/`conf.route6_autoload`, whether the OS table is read on first access. `conf.l3socket`, `conf.l2socket` and `conf.l2listen` — with scapy's `L3socket`/`L2socket`/`L2listen` spellings — now name **real socket classes** (`python/wiry/supersocket.py`) and take a replacement, which is what an `Automaton` opens; `None` puts wiry's own back, so scapy's save-and-restore idiom still works. **`conf.route` and `conf.route6` are the kernel's tables** (E26). Reporting rather than choosing: `conf.use_pcap` is True exactly when the `live` feature is on, because libpcap is the only backend there is | Nothing here is inert, which is the rule the object is built on: a knob that silently absorbed a setting would be worse than a missing one. `__slots__` means a name `conf` does not have raises `AttributeError` instead of being swallowed, and `conf.use_pcap` raises `NotImplementedError` naming libpcap if set to anything but what the build is. `conf.route` and `conf.route6` have no setter, so a caller cannot install a whole table wiry would then be believed for; the tables themselves are freely edited through `add`/`delete`/`ifadd` and re-read with `resync()`. Remaining, each for a reason rather than for want of typing: `conf.ifaces` (scapy's registry of `NetworkInterface` objects — wiry answers interfaces as dicts from `interfaces()` and has no such object model), `conf.color_theme` (no themed output), `conf.interactive`/`conf.interactive_shell` (wiry is a library and launches no shell), `conf.manufdb` (no OUI database is shipped), `conf.mib` (no SNMP MIB), `conf.netcache` (E18 keeps no cache on purpose, since a stale entry answering for a host that moved is worse than a second request) and `conf.checkIPsrc` (the matching knob it would drive does not exist in `answers.rs`). Each of those raises `AttributeError` | PARTIAL |
| E17 | `send` and `sendp` take `x`, `inter`, `loop`, `count`, `verbose`, `return_packets` and `iface`. The whole list is serialised in the facade and crosses into Rust once; the repeat and the `inter` pacing happen there, so a `sendp` of a thousand frames is one crossing. `loop` repeats until interrupted, with a signal check between passes. `send` writes IPv4 datagrams to a raw socket with the header included, letting the kernel route and resolve | Remaining, all deliberate: `send` is IPv4-only and an IPv6 packet raises `NotImplementedError` naming `sendp` — raw bytes as well as a built packet, since past the facade the only thing between them and the `AF_INET` socket is a version nibble — because the raw socket is `AF_INET` and a second address family is a second backend; anything `send` is handed that is not an IPv4 datagram (an `Ether()`-wrapped one meant for `sendp`, a line of text) raises `ValueError` rather than being written to the wire with a destination read out of octets 16..20, and the check reads RFC 791's version, IHL and total length directly, because the dissector is *told* the link type rather than asked to detect it and so reports IPv4 for any buffer long enough; anything that is not a packet, bytes or a `str` raises `TypeError`, since `bytes(3)` is three zero octets and `sendp([1, 2, 3])` would otherwise put three all-zero frames on the wire; `inter=` must be finite and raises `ValueError` otherwise, because a pause of `Duration::MAX` is a hang; raw IPv4 sending is refused outright on Windows, where it is restricted by the OS; `socket=` and `realtime=` raise `NotImplementedError` rather than being ignored, and `iface_hint=` is accepted and unused; `return_packets=True` returns a plain list of what was handed in, not a `PacketList`, since the sent frames are never indexed into a capture buffer; `verbose` prints one summary line rather than scapy's dot per packet | PARTIAL |
| E18 | The active tools are implemented on top of `sr`/`srp`: `traceroute` (with `TracerouteResult` and its `show()`, `get_trace()`, `summary()`, `nsummary()`), `arping`, `srloop`, `srploop`, `getmacbyip` and `get_if_hwaddr`. Every one is an exchange plus arithmetic, and the arithmetic is kept in plain functions over plain data — which probes a sweep builds, which reply belongs to which hop, what a round prints, what a CIDR expands to, what a multicast group maps to — so `tests/test_tools.py` exercises all of it from canned packets with no interface and no privileges. Only the exchange needs the `live` feature. Privileged round-trip checks are in `dev/live/`: `netns_check.py` runs the tools over the veth pair where exactly one address answers, and `tools_check.py` runs them against real routers and hosts | `traceroute` is **one** `sr` over the whole TTL sweep, not one exchange per hop, so a 30-hop trace costs one timeout. It sets `IP.id` to the TTL on each probe: `answers.rs` matches an ICMP error on the quoted IP id but not on the quoted TTL, so without that every hop's time-exceeded would pair with the first probe and the trace would be one row long. `show()` renders a `*` for a TTL between two answered hops that nobody answered, so a gap is visible rather than implied by a jump in the numbering, and stops at the last answer — the probes past it are counted in `unanswered`. `arping` expands a CIDR itself, with `ipaddress` from the standard library, rather than through the generators of E21: a sweep pairs each address with its own reply, which is arithmetic the tool owns rather than a template's product. A sweep wider than 65,536 addresses raises rather than hanging, and the count is checked before the range is walked. `srloop`/`srploop` are ordinary synchronous loops and own no thread: a Rust thread calling into Python after finalisation segfaults, which is the failure `panic = "abort"` is kept off to prevent, so there is deliberately nothing here to join. Ctrl-C during a round is answered by `sr_live`'s 250 ms signal slices and during the pause between rounds by the pause being taken in slices of the same length; either way the loop returns the rounds it has already collected instead of losing them. `traceroute` resolves a hostname through `socket.gethostbyname` — in the tool, never in a field, so E13's rule that a name in an address field is refused still holds. Remaining, all deliberate: `traceroute` is IPv4 only, because E14 has no reply rule for an ICMPv6 error; there is no `TracerouteResult.graph()`, no AS lookup and no `conf.netcache`; the tools still send what the kernel routes rather than consulting `conf.route` themselves, which E26 now makes possible and E18 has not yet taken up, and `show()` is matched by eye like every other renderer (E4); `getmacbyip` keeps no cache, since a stale entry answering for a host that has moved is a worse failure than a second request; `srloop`'s per-round report is one line per answer rather than scapy's exact wording; `arping(cache=)` is accepted and unused | PARTIAL |
| E19 | Fragmentation and reassembly are implemented (`crates/wiry-core/src/frag.rs`). `fragment(pkt, fragsize=1480)` splits an IPv4 datagram per RFC 791 §3.2: offsets count 8-octet units, MF is set on every fragment but the last and OR'd with the incoming MF so a fragment of a fragment stays marked, Total Length, IHL and the header checksum are recomputed per fragment, and only the options RFC 791 §3.1 marks copied (the high bit of the type octet) reach the later fragments, padded out to the 32-bit word IHL counts. `defragment(plist)` returns the list with datagrams put back together and `defrag(plist)` scapy's `(not fragmented, reassembled, still incomplete)` split; both key on source, destination, identification and protocol. Over a `PacketList` reassembly is a bulk path — the capture is already one buffer in Rust, so it is one crossing with the GIL released and a new capture comes back — and a plain list of packets goes through the same engine in one list-granularity call. The suite asserts the two produce identical octets. `fragment6`/`defragment6` do the same for RFC 8200 §4.5, inserting and removing the Fragment extension header and computing the unfragmentable part locally (hop-by-hop, a Destination Options header only where a Routing header follows it, and Routing) | Reassembly buffers attacker-controlled bytes, so the bounds are stated rather than implied: a datagram cannot exceed `MAX_DATAGRAM` (65,535, RFC 791's Total Length), a run cannot hold more than `MAX_INFLIGHT` (1,024) unfinished datagrams or `MAX_BUFFERED` (16 MiB) of them, and one datagram cannot be fed more than `MAX_FRAGS` (8,192) fragments — the most a real one needs. Exceeding a bound evicts the oldest datagram, whose fragments are then reported incomplete rather than dropped silently. What a *claim* costs is bounded too, and counted the same way as E25's: the received-range list is a `BTreeMap`, so a claim is found, merged and reinserted in O(log h) plus one step per range it swallows, and a range is swallowed once. It was a sorted `Vec` until this was measured, and a `Vec` shifts every range above the insertion point: fragments arriving in *descending* offset order cost 2,000 entries touched each at 4,000 fragments and 4,000 each at 8,000, which is quadratic and remotely triggerable. The timed guard that stood here saw none of it — it fed ascending offsets, the one order a `Vec` inserts into for free, and its twenty-second deadline also passed with the original re-sorting bug measured back in at 1.5s. **Overlapping fragments resolve first-writer-wins**: the earliest fragment to claim a byte range owns it and a later one overwrites nothing. That is a choice, not a standard — operating systems disagree here and the disagreement is the whole basis of fragmentation-based IDS evasion — so a capture wiry reassembles may differ from what a given target host would have seen. Remaining: `fragment` honours Don't Fragment by returning the datagram whole rather than raising, which differs from a router's duty to discard it and send ICMP 3/4; a reassembled datagram keeps the first fragment's IP header with its checksum recomputed, so a datagram whose original checksum was wrong does not round-trip; a link-layer trailer on the input frame is dropped; transport checksums are never recomputed, which is correct for a reassembly and wrong if you then edit the result; there is no timer, because a capture has no clock of its own — eviction is by count and by bytes, not by age; a datagram that is itself a fragment (More Fragments set, or a non-zero offset) is still split, which RFC 791 §3.2 allows, but the pieces do not reassemble: nothing in them says where the outer datagram ends, so they are reported incomplete rather than guessed at; `fragment6` refuses a datagram whose unfragmentable part is already `MAX_EXT` (8) extension headers deep, since the Fragment header it would insert sits past where reassembly walks, and emitting fragments this module could not put back is worse than emitting none; a datagram already carrying an IPv6 Fragment header is not re-fragmented, since RFC 8200 §4.5 gives the Identification to the originating node; and that Identification is derived from the datagram's own content so that `fragment6` stays reproducible, where scapy draws it at random | PARTIAL |
| E20 | Capture writing is streaming as well as bulk. `PcapWriter(filename, linktype=None, gz=False, endianness="", append=False, sync=False, nano=False, snaplen=65535, bufsz=4096)` takes a packet, a list, a `PacketList` or raw bytes per `write()` call, plus `flush()`, `close()` and the context-manager protocol; `PcapNgWriter` fixes the format, and `wrpcap()`/`wrpcapng()` are one-call wrappers over it that also take `append=True`. The format follows the file name (`.pcapng`/`.ntar`, `.gz`) unless `pcapng=`/`gz=` overrides it. The link type is settled from the first packet written and the header goes out then, so a writer opened before its packets exist still declares what it ends up holding; appending checks the existing file's format, link type, timestamp unit and byte order and refuses a mismatch rather than writing frames its header misdescribes (e2e4361 is the precedent), and for pcapng also checks that the file ends on a whole block — the trailing total length must locate a block header that agrees with it — because appending behind a half-written block buries both it and everything added after it. A refused append never writes: the existing bytes are untouched on every rejection path. Classic pcap gets no equivalent tail check, because its records carry no trailing length and nothing anchors the end of the file — finding a half-written final record means walking every record header from the front, which an append to a 368 MB capture should not have to pay — so appending behind a truncated pcap record still produces a tail no reader frames correctly. A compressed or file-object target is rewritten whole, through a temporary file and one `os.replace`, so a delivery that fails partway leaves the previous capture intact rather than truncated. `pkt.wirelen` carries the wire length through a round trip: the record index keeps `origlen` beside the captured length, so copying a snaplen-clipped capture writes the original wire length rather than collapsing it to the captured one, and a writer left open is closed by an `atexit` hook before finalisation, where `tempfile` and `gzip` can no longer be imported. `write(PacketList)` copies the whole capture inside Rust with the GIL released and mints no Python object — 791,615 packets, 368 MB, byte-identical, in 0.09 s, best of five into a warm page cache — while `write(pkt)` crosses per packet, which is what a streaming writer is for. Both encode through the same `wiry-core` functions, and `tests/test_pcap_write.py` asserts the two produce the same file byte for byte | Remaining: `endianness=">"` raises `NotImplementedError`, since output is little-endian only; `bufsz` is accepted and ignored, the Rust sink doing its own buffering; a gzipped or file-object target is held in memory until `close()`, because the compressor is Python's, so a gzipped capture costs its uncompressed size in RAM and appending to one rewrites the whole file; a packet block written from a `PacketList` always names interface 0, so a pcapng whose interfaces declare different `if_tsresol` values is rewritten with the first one's unit for every record (the same single-flag limit the read side has, E5); `sync=True` flushes and `fsync`s once per `write()` call rather than once per packet; a timestamp that reaches the writer as `pkt.time` is a Python float, so at a present-day epoch it resolves to about 240 ns and `nano=True` cannot round-trip a nanosecond that the float never held — the file-to-file bulk path is exact, since it copies the stored integers without going through a float, and scapy avoids this by carrying `time` as a decimal; scapy's `RawPcapWriter`, `RawPcapNgWriter`, `.f` and `.fileno()` are not provided | PARTIAL |
| E21 | Generators: one declaration that multiplies into many packets. A field may hold a list (`IP(dst=["a","b"])`), a two-element tuple read as an inclusive range of integers (`IP(ttl=(5,10))`) or of addresses (`IP(dst=("192.168.0.100","192.168.0.199"))`), a `Net`/`Net6` (which also parse from a CIDR string written straight into an address field, `IP(dst="10.0.0.0/24")`), or a volatile value — `RandNum`, `RandByte`, `RandShort`, `RandInt`, `RandLong`, `RandIP`, `RandIP6`, `RandMAC`, `RandString`, `RandBin`, `RandChoice`, `RandEnumKeys`. A packet carrying any of them is a *template*: iterating it yields the cartesian product in scapy's order (within a layer the last field declared varies slowest; an enclosing layer varies slower than the one it carries), a volatile field multiplies by one and is drawn afresh per packet, and a list may hold ranges, so `IP(ttl=[1,2,(5,9)])` is seven packets. `fuzz(pkt)` randomises every field that is not computed, not conditional and not already set. `corrupt_bytes` and `corrupt_bits` are there too. **The description is data, never a callback**: Python hands the whole generator set over in one crossing and Rust walks the product (`wiry-core/src/generate.rs`, `wiry-py/src/template.rs`), so `sendp(Ether()/IP(dst=Net("10.0.0.0/8")))` is one crossing that expands 256 frames at a time rather than sixteen million crossings or sixteen million frames in memory. Iteration is lazy in 256-packet chunks, so `IP(src=Net("10.0.0.0/8"), dst=Net("10.0.0.0/8"))` reports 2^48 packets and an `islice` of three off the front costs one chunk of 256 realisations (0.15 ms), not 2^48. Volatile values come from a seedable splitmix64 in Rust, and `set_rand_seed(n)` makes a whole fuzz run repeat, which scapy cannot do. Eight expansion routes — iteration, `expand`, `wrpcap`, `bytes()`, a whole-product chunk, one index at a time, seven-packet chunks and the Rust send path — are asserted to produce identical octets, since a bulk path that disagrees with the per-packet path is the bulk path being wrong | Remaining, all deliberate: a helper that must hand back real objects — `wrpcap`, `sr`, `srp`, and the list form of `send`/`sendp` — refuses a template of more than 2^20 packets rather than materialising it, naming iteration in the message; only `sendp(template)` and `send(template)` stream unbounded. `fuzz()` takes a packet being built and raises `NotImplementedError` on a dissected one, since E12's note applies: a materialised packet cannot be turned back into a field spec. `fuzz()` skips conditional fields (E1) rather than choosing among the layouts they describe, skips variable-length regions, so an option region and a `StrField` stay at their defaults, and *does* randomise `ihl` and `dataofs`, which scapy leaves computed — the result is a header whose length field may disagree with the header, which is a legitimate fuzz case and a difference. `Net` iterates plain strings, so it carries no per-address `.scope`, and `scope=` is stored but never used. A generator in an option list or a `Raw` payload is not recognised; the value has to be on a flat field. A tuple of two strings is a range only when both parse as addresses, and a CIDR string is read as a `Net` only in an address field — elsewhere a slash is just a character. **The seed is one process-wide stream, not a per-template one**: `set_rand_seed(n)` replays a run that makes the same draws in the same order, and that is all it promises. Two threads drawing at once interleave, and five identically-seeded four-thread runs produced five different results, so the guarantee is false under concurrency — a `sendp` of a volatile template on a background thread included. A per-thread stream was rejected because seeding on the main thread would then silently not reach a worker. Counts are `u128` and saturate: `Net6("::/0")` is 2^128 addresses, one more than a `u128` holds, so that template reports 2^128-1 and its last address is unreachable; anything wider reports `u128::MAX`. Saturation cannot smuggle a huge product under the 2^20 eager-helper threshold, because it only ever saturates *upward* and no generator can count zero — every empty spelling raises at construction. An index past the product is refused rather than wrapping to the start. `send()` of a template checks that every datagram is IPv4 chunk by chunk rather than before the first octet, so an unsendable packet at position 300 is refused only after the first 256 are already on the wire. `fuzz()` also randomises a field whose value the payload binds — `fuzz(UDP()/NTP())` draws a random `dport`, where scapy leaves the bound port alone — so a fuzzed packet re-dissects to `Raw` rather than back to its payload layer, which is one of the two generator failures in `dev/scapy_suite.py`. The other cannot pass at all: `Test corrupt_* functions` seeds the interpreter's `random` and asserts the exact octets `corrupt_bytes` returns, which pins scapy's own PRNG call order — its implementation rather than its interface, and the very coupling `set_rand_seed` exists to break | PARTIAL |
| E22 | `json()`, `command()` and `hexdiff()` exist. `command()` has two modes and both are honest: a packet still under construction prints back only what was assigned to it, in the order the layer declares its fields, which is what scapy prints and what its regression suite asserts; a dissected packet prints every field the header carries, computed ones included, because a supplied value is honoured rather than recomputed and that is what makes `eval(pkt.command())` byte-identical. The suite asserts that round-trip over built packets, hand-built RFC vectors and 8,572 packets of a real capture | Remaining: `json()` is a flat object keyed by layer name (a repeated name is suffixed with its position), where scapy nests each layer under `"payload"` and writes `null` for a computed field nobody set; byte values are hex strings, since JSON has no bytes. `hexdiff()` aligns the two inputs with `difflib` and marks differing rows at the start of the line, which is not scapy's column layout: the output is for reading, not for parsing, and past `ALIGN_LIMIT` (4,096 octets) the columns line up by offset instead, because the alignment is quadratic. `command()` of a packet using a declared layer (E3) evaluates only where that class is in scope. Dissection stops at 32 layers, so a chain deeper than that leaves a tail no layer named; `command()` and `json()` emit it as a trailing `Raw` rather than dropping it, which keeps the round-trip byte-identical but does not recover the layers. `hexdiff()` leaves difflib's `autojunk` heuristic on: without it a run of one octet — zero padding, which most packets carry — makes the alignment quadratic, 1.7s for a 4 KB pair against 1.2ms with it for the same opcodes | PARTIAL |
| E23 | `sprintf` implements the format language: `%field%`, `%Layer.field%`, `%Layer:field%`, `%Layer:nb.field%`, `%.time%`, `%%` for a literal percent, the printf-style modifiers (`%02x,IP.ttl%`, `%-5s,IP.src%`, `%#05xr,TCP.sport%`) with the trailing `r` raw flag, and `{Layer:...}` conditional blocks, which nest. A missing layer or field renders as `??`, as scapy's relaxed default does. `show2()`/`show2_str()` build the packet and dissect the result, so the lengths and checksums shown are the ones that will be sent. `PacketList.sprintf(fmt)` renders a whole capture in one pass: the format is compiled to the set of columns it names and pulled through `columns()`, so no packet is materialised, and the suite asserts it agrees character-for-character with the per-packet path | Remaining: wiry has no enum-name rendering, so `%IP.proto%` is `6` where scapy prints `tcp` and `%Ether.type%` is `2048` where scapy prints `0x800` or `IPv4`; the `r` flag is therefore accepted and parsed but changes nothing, since the raw and rendered forms are already the same value. `{Layer.field:...}` is a wiry extension meaning "this layer carries this field", which scapy renders as the empty string. A format modifier over a missing value is dropped rather than raising, where scapy raises `TypeError`. `sent.time` does not exist, because a sent packet is never indexed into a capture. A bare `%field%` or an occurrence other than the first depends on the individual packet's layer chain, which a column does not carry, so `PacketList.sprintf` falls back to the per-packet path for those formats rather than answering differently, as it does for the DNS record sections (`qd`/`an`/`ns`/`ar`), which E8 says a parser serves and no column can carry. A `{Layer:` nested past 64 deep is literal text, because rendering recurses once per level and the format string is not always the caller's own | PARTIAL |
| E24 | `PacketList` aggregation: `summary(prn=, lfilter=)`, `nsummary(...)`, `show(...)`, `sessions(session_extractor=)`, `conversations(getsrcdst=)`, `make_table(fn)` and `plot(fn)`. The default `summary` and `sessions` paths stay in Rust: summaries are built from layer spans with the GIL released and cross once, and a session's membership never leaves Rust at all — keys are computed there, and a flow becomes a `PacketList` view over the shared capture buffer only when the caller asks for that flow. On a 50,000-packet slice of `bigFlows.pcap` that is 12x scapy's `sessions()` with both captures already read, 64x counting the read scapy needs first, and it finds the same 5,820 flows. The default key is the address tuple in scapy's spelling (`TCP 10.0.0.1:1000 > 10.0.0.2:80`, `ICMP a > b type=8 code=0 id=0x1234`, `ARP a > b`, `Ethernet type=0800`, `Other`) | Remaining: `sessions()` itself still only groups — reordering and stream rebuilding are E25's, reached through `streams()` and `TCPSession`. Two key strings differ from scapy's on real traffic and nothing else does: `proto=2` where scapy names the enum (`proto=igmp`), and an ICMP type with no identifier (types 3, 11, …) omits the `id=` clause where scapy prints `id=0x0`, because E1's conditional fields say the field is not there. wiry keys a bare `IP()/TCP()` on its addresses where scapy, which requires an `Ether` layer to look further, answers `Other`. `sessions()` returns a lazy `Mapping`, not a `dict`, so it can mint views on demand; `conversations()` returns graphviz DOT source rather than displaying an image, and renders through the `dot` program only when given a `target=` — graphviz is not a dependency and neither is matplotlib, which `plot()` imports lazily. `make_table` returns the table as a string instead of printing it, and `make_lined_table`/`make_tex_table`/`timeskew_graph` do not exist. `summary` and `nsummary` print, as scapy's do; `wiry.report.summary_lines` is the non-printing form. `conversations()` reads its address pairs as columns and so materialises no packet either, but `make_table(fn)` and `plot(fn)` take a per-packet callback by contract and cross per packet, exactly as a `prn` does | PARTIAL |
| E25 | TCP stream reassembly (`crates/wiry-core/src/stream.rs`). `PacketList.streams()` reassembles every stream in a capture in one crossing with the GIL released and hands back a lazy `Mapping` of the same key `sessions()` uses to a `TCPStream`: two directions, each with `data` (octets in sequence order), `segments` (`(offset, length, packet)`, so a stream offset maps back to the packet it came from), `gaps`, `flags`, `app`, `messages()` and `parsed()`, plus `packets()`, a no-copy view over the packets that contributed. Sequence numbers are read onto a 64-bit line derived from the frontier, so a capture that spans the 32-bit wrap reassembles; retransmissions are delivered once, out-of-order arrival is put back in order, and a hole is recorded as a `Gap` rather than filled in. `sniff(offline=..., session=TCPSession)` is the scapy-shaped route to the same engine: every complete HTTP message and TLS record a stream reassembled to comes back as a packet carrying its first segment's Ethernet, IP and TCP headers with the reassembled octets as payload — lengths and the IPv4 header checksum recomputed — and everything else passes through as captured. `IPSession` and `DefaultSession` are there too, and `session=` runs after the capture filter, where scapy runs one. On `bigFlows.pcap` (791,615 packets) this finds 22,312 streams in 1.4s and 98,687 complete TLS records and 13,721 complete HTTP messages, of which **29% and 37% respectively span more than one segment** and so are invisible to any single-packet dissector; the largest reassembled HTTP message is 1.78 MB. | **The bounds are named, and reassembly is deliberately unable to outgrow its input**: nothing stands in for octets that never arrived, so a half's `data` is octets that were actually captured and a run's output can never exceed the frames fed to it. On top of that: `MAX_STREAMS` (65,536) concurrent streams, past which the least recently active is finished and returned with `evicted` set rather than dropped; `MAX_STREAM` (16 MiB) delivered octets per direction, past which it reports `TRUNCATED`; `MAX_HELD_SEGS` (64) out-of-order segments and `MAX_HELD_BYTES` (256 KiB) per direction and `MAX_HELD_TOTAL` (32 MiB) across all of them, past which the frontier is forced over the missing octets, which records a `Gap` and delivers what was waiting behind it; `MAX_AHEAD` (1 MiB) past the frontier a segment may claim, beyond which it is refused and `LOSSY` is set; and `MAX_REFRAME_GROWTH` (64 MiB) of framing the `session=` splice may add to the capture it was given, past which messages are left unframed and their packets pass through as captured. That last bound is the one part of reassembly that can outgrow its input, because each framed message costs a frame of headers and a TLS record can be five octets: unbounded, a segment packed with empty records came back about twelve times its own size. Inserting a segment is O(log h) in the segments held plus the octets it contributes, with h bounded by `MAX_HELD_SEGS`; measured on six crafted worst cases (descending segments, descending overlapping segments, a wide segment repeatedly laid over many sparse held ones, one stream per packet, endless retransmission, a stream that never becomes contiguous) the per-segment cost is 82-183 ns and flat within noise from 4,000 to 64,000 segments, except one stream per packet, which rises from 460 to 914 ns because 64,000 streams is where `MAX_STREAMS` eviction begins and which falls back to 796 ns at 512,000. That scaling is asserted on stable, and it is **counted rather than timed**: the reassembler tallies the entries an arrival examines and the octets it copies, and the test reads the tally. A wall-clock ratio could not do the job — a quadratic term shows as sixteen times the work, so a threshold has to sit under sixteen, and a loaded machine's noise reaches past twelve, which is what made the timed version flaky. The counted bound is stronger as well as stable: an insert may examine at most sixteen entries per arrival against a measured worst of seven, so an insert that walked what it holds fails even where the total stays linear, and the octets moved are held to twice the octets received, which is exactly the invariant that an arriving octet enters the held set once and leaves it once. The counter is a zero-sized type outside `cfg(test)`, so the measured path carries nothing, and the assertion lives in `crates/wiry-core/src/stream.rs` for that reason. **Overlapping octets resolve first-writer-wins**: the earliest segment to claim a stream offset owns it and a later one overwrites nothing. That is a choice, not a standard — operating systems disagree here and the disagreement is the basis of stream-based IDS evasion, and TCP is worse than IP's case because a sender also controls the window, the segment size and when it retransmits — so what wiry reassembles is *a* reading of the stream, not necessarily the one a given target host would have assembled. Remaining: there is no timer, because a capture has no clock of its own — eviction is by count and by bytes, not by age, and a stream is never closed early on FIN or RST, so a port pair reused inside one capture comes back as one stream unless the concurrent-stream bound intervened. The origin of a direction is the earliest sequence number in its first eight segments (or the ISN when a SYN is captured); a segment earlier than that, arriving later still, cannot be delivered, and is refused with `LOSSY` set and its octets counted in `dropped` rather than passed over as a retransmission. `session=` is two contracts, and a session says which it is. scapy's per-packet `process(pkt) -> Packet | None` runs between the capture filter and the callbacks, **on an interface as well as over a file**: it rides the `wrap` hook, so `lfilter`, `prn` and `stop_filter` all see what the session produced and `count` counts only what it let through, and the FFI rule is untouched because the packet already exists by the time the session sees it. wiry's own reassembling sessions are the other contract — `needs_capture = True` and a `bulk_process(pl)` that is one Rust pass over the whole capture — and those stay offline only; a live sniff raises, and their `process(pkt)` refuses by name rather than approximating in Python what the Rust engine does. `DefaultSession` takes scapy's `supersession=`. A per-packet session that *replaces* a packet is refused with `store=1` rather than quietly dropping the replacement, because the `PacketList` sniff returns is a view over the capture buffer and a synthesised packet was never in one; with `store=0` it works, and `prn` sees every replacement. There is no `NetflowSession`, since wiry has no Netflow template state. TCP checksums are never verified, so a corrupt segment is reassembled like any other. **A spliced frame’s TCP checksum is 0**, which is P8’s "not computed": the octets behind those headers were never one segment, so the captured value would report as valid over bytes it never covered — a wrong answer a downstream tool believes — and a recomputed one would assert that this frame was really transmitted, which is false. C1 does not govern here: its rule is to leave a checksum as the wire gave it, and a spliced frame was never on a wire. The IPv4 header checksum *is* recomputed, because that header genuinely describes the frame it is in. A segment inside an IP fragment is not reassembled into its stream — defragment first. A header block is read through the dissector’s own parse (`text::message_headers`, which HTTP, SIP and the framer all go through), so a folded or oddly-spelled header cannot frame one message and display another; the 256-line cap that parse carries applies to both, so a header past it is invisible to framing exactly as it is to `show`: a response with its `Content-Length` down there never completes and a request frames as though it had no body. No octet is lost either way — what no message claims is framed on its own. Only HTTP/1.x and TLS records are framed into messages; every other application protocol reassembles to octets with no message boundary, an HTTP response with neither `Content-Length` nor chunking runs to a close nothing announces and so never completes, and a `HEAD` response or a 204 is framed by its `Content-Length` as if it had a body. There is still no incremental Python-visible *reassembler*: the engine is incremental in Rust and the suite drives both paths, but Python reaches the reassembly itself only in bulk, which is why `TCPSession` and `IPSession` have no per-packet form. | YES |
| E26 | **The routing tables are the OS's** (`python/wiry/route.py`, transcribed from `scapy/route.py`, `scapy/route6.py` and `scapy/arch/unix.py`). `conf.route` and `conf.route6` read the kernel table on first use — `/proc/net/route` and `/proc/net/ipv6_route` on Linux, `netstat -rn -f inet` and `-f inet6` on BSD and macOS — and answer `route(dst) -> (iface, source address, gateway)` by longest matching prefix with the metric as the tie-break, including the loopback answer for one of our own addresses and for `127.`/`::1`. `add`, `delete` (scapy's `delt`), `ifadd`, `ifchange`, `ifdel`, `get_if_bcast`, `make_route`, `resync()` and a rendered `repr` are all there, and `read_routes`, `read_routes6` and `in6_getifaddr` are exported. Interface addresses come from `SIOCGIFADDR` through `ioctl` and IPv6 addresses from `/proc/net/if_inet6` or `ifconfig`, so **none of this needs libpcap, the `live` feature or a privilege** — a build with no capture backend still routes | This replaces a read-only view that synthesised one host route per interface address and was documented as unusable for a routing decision. **On macOS wiry disagrees with scapy, and the kernel agrees with wiry.** scapy reads Darwin's table through `PF_ROUTE` sysctl and parses the truncated `RTA_NETMASK` sockaddr without padding it, which yields masks like `240.255.255.0` for `224.0.0.0/4` and `255.255.255.255` for `10.0.0.0/24`; on the machine this was measured on scapy therefore sent `192.168.64.5` out the default gateway and `169.254.1.1` via a router, where `route -n get` names `bridge101` and a direct `169.254.0.0/16` — which is what wiry answers. Both tables hold 25 routes and agree on every other destination tried. Remaining: `add`/`delete` change wiry's copy and never the kernel's, deliberately — a packet library decides where to send its own packets and does not reconfigure the host; Linux is read from `/proc` rather than netlink, so a route in a table other than `main` and a multipath route's second leg are invisible, and `/proc/net/ipv6_route` has no source address of its own so the candidate set is built from the interface's addresses; IPv6 source selection is RFC 3484 §5 reduced to scope match then longest common prefix, without scapy's 6to4 and temporary-address tie-breaks; an unrecognised platform falls back to one host route per interface address, which is the old behaviour and is marked as such; and there is no `conf.netcache`, no `Route.ifchange` cache flush beyond dropping wiry's own, and no Windows table reader | PARTIAL |
| E27 | **State machines.** `Automaton` and the `ATMT` decorators (`python/wiry/automaton.py`, transcribed from `scapy/automaton.py`): `state` with `initial`/`final`/`stop`/`error`, `condition`, `receive_condition`, `ioevent`, `timeout`, `timer`, `eof` and `action`, all with priorities and all overridable in a subclass; `run`, `runbg`, `next()`, `stop`, `forcestop`, `restart`, `destroy`; breakpoints, single-stepping and interception points; `master_filter`, `store`, `update_sock`, `timer_by_name`, `as_supersocket`, `spawn` and `build_graph`. Packets come from and go to a socket object (`python/wiry/supersocket.py`): `L2Socket`, `L2ListenSocket` and `L3Socket` over wiry's capture backend, `StreamSocket` over an ordinary TCP socket, and **`OfflineSocket`, which reads a capture and records what was sent instead of transmitting it**. That last one is why `tests/test_automaton.py` drives every state, transition, timer, io event and reply from canned packets with no interface, no root and no `live` feature | `Automaton` is callback-driven and per-packet by contract, exactly as `sniff`'s `prn` is, and the FFI rule is untouched: the session or condition sees a packet the Rust dissector has already produced, and no Python runs inside the dissection loop. The control thread is a daemon `threading.Thread` registered in a `WeakSet` that an `atexit` hook force-stops, and a live socket's `AsyncSniffer` is stopped and joined on close through the same idiom, because a Rust capture thread calling into Python after finalisation segfaults past the protection `panic = "abort"` is kept off to provide. **`Automaton.packets` is a plain `list`, not a `PacketList`**: a `PacketList` is a view over a capture buffer and packets a machine sends were never in one. `graph()` returns graphviz DOT source and renders only when given a `target=`, as `conversations()` does (E24), since graphviz is not a dependency. **`build_graph` terminates where scapy's does not**: scapy re-expands a name indirection without remembering which names it has followed, so a method whose body names itself — `self.tick += 1` inside a method called `tick`, which is the shape of scapy's own graph test — loops forever; wiry expands each name once. **A socket that cannot be opened is raised out of `start()`, not hung on**: scapy sets the control thread's ready event only after both sockets are open, so a `recvsock` that raises kills the thread and leaves the caller waiting on an event nothing will ever set — which is what `Automaton()` with no `ll=`/`recvsock=` does in a build with no `live` feature. Remaining: no `pipetool`; `spawn` serves TCP only; `sniff(opened_socket=)` is refused rather than implemented, because the sniff state machine is one implementation in Rust and reading a Python socket through it would mean a second one that could disagree — `Automaton(recvsock=...)` is the surface that takes such an object | PARTIAL |
| E28 | **Answering machines.** `AnsweringMachine` (`python/wiry/ansmachine.py`, transcribed from `scapy/ansmachine.py`) with `filter`, `is_request`, `make_reply`, `send_reply`, `print_reply`, `parse_options`, the sniff/send option split, `bg()` and publication under `function_name`; `AnsweringMachineTCP` and `AnsweringMachineUDP` serve ordinary sockets, one thread per client. `replies_to(packets)` runs the whole reply path over a list and sends nothing, which is what makes `tests/test_ansmachine.py` privilege-free | The reply arithmetic is where the machine's content is, and it is plain functions over plain packets, exactly as E18's tools are. Remaining: none of scapy's concrete machines (`ARP_am`, `BOOTP_am`, `DNS_am`, `ICMP_am`, …) are shipped, only the framework they are written in; the TCP and UDP machines read their client sockets directly rather than through `sniff`, because `sniff(opened_socket=)` is refused (E27) | PARTIAL |

## Protocol coverage (the sixty layers generated from `dev/protogen/specs/`)

Sixty new `ProtoId`s were added, covering every protocol on the parity list.
`dev/protogen/README.md` explains the tool; this table is what the layers do and
do not do. **A layer that dissects its header and leaves its body as `Raw` is
listed here as exactly that.**

| # | Item | Remaining | Status |
|---|---|---|---|
| P1 | Sixty layers, all from their RFC or registry, all cited in the module and in the spec: LLC, SNAP, STP, LLDPDU, CDP, RadioTap, Dot11 with its six management bodies, SCTP, IGMP, the five RFC 4861 Neighbor Discovery messages, the four MLD messages, ESP, AH, OSPF_Hdr, RIP, BGPHeader, VRRP, HSRP, BFD, NTP, DHCP6, SNMP, TFTP, Syslog, NBNS, NBTSession, Radius, RTP, RTCP, NetflowHeaderV5, NetflowHeaderV9, IPFIX, SFlow, QUIC, WireGuard, TLS, HTTP, SSH, MQTT, ModbusADU, SMB2_Header, LDAP, SIP, FTP, SMTP, IMAP, Telnet. Dispatch reaches them from EtherType, the 802.3 length framing, the LLC SAP, the SNAP protocol id, the IP protocol number, the ICMPv6 message type, and UDP and TCP ports | Depth varies and is stated per row below. Nothing here recomputes a checksum or a length (P8) | PARTIAL |
| P2 | The encapsulations — IPv6 extension headers, GRE, VXLAN, GENEVE, IP-in-IP, 6in4, 4in6, MPLS, PPPoE/PPP, GTP-U, ERSPAN — are not generated layers and are not in this table | They are hand-written and walked through to the inner transport; E6 states where each of them stops | CLOSED |
| P3 | **Fixed-size record arrays stay in the payload.** A protocol whose body is `n` copies of a fixed record is dissected as far as the count, and the records themselves are `Raw`: RIP route entries (RFC 2453 §3.6), NetFlow v5 flow records, sFlow v5 samples, IGMPv3 group records (RFC 3376 §4.2.1), MLDv2 multicast address records (RFC 3810 §5.2.4), OSPF bodies (RFC 2328 §A.3.2 onwards), BGP message bodies (RFC 4271 §4.2 onwards), SCTP chunk *parameters* (the chunks themselves are parsed) | The flat model has no repeating-group construct (E1), and `options.rs`'s named-item shape is a poor fit for a homogeneous array. Reaching these needs a third value shape, which is a larger decision than this branch should take alone | OPEN |
| P4 | **Signed fields are read unsigned.** `FieldKind` has no signed integer, so NTP's `poll` and `precision` (RFC 5905 §7.3, both signed exponents) read as 0..255 | A scripting user comparing `pkt[NTP].precision` to a negative number gets the wrong answer. Adding `FieldKind::Int` is a small change but touches `field.rs`, `show.rs` and `parse.rs`, which are shared files this branch was told not to churn | OPEN |
| P5 | **RTP and RTCP are bound to no port.** Both are negotiated out of band (SDP), and every heuristic for finding them on an arbitrary even UDP port has a false-positive rate this dissector will not accept | They are reachable by `bind_layers(UDP, RTP, dport=...)` and by direct construction, and not otherwise. Dissecting a capture of RTP therefore takes one line of setup | OPEN |
| P6 | **NetFlow v9 and IPFIX data records need the template the exporter sent earlier.** That is per-session state across datagrams, which this dissector does not keep and which the FFI boundary rule makes expensive to keep in Python | Only the export header is dissected; the FlowSets and Sets are `Raw`. This is a real limitation, not a shortcut: template-based decoding is a stateful feature, not a layout | OPEN |
| P7 | **Encrypted payloads stay `Raw`, by construction.** ESP (RFC 4303 §2) past the sequence number, QUIC (RFC 9000 §12.1) past the header, WireGuard (§5.4.6) past the counter, and every TLS record body that is not a ClientHello or ServerHello | Not a gap that can be closed without keys | CLOSED |
| P8 | **No new layer recomputes a length or a checksum.** `compute.rs` handles IPv4, IPv6, TCP, UDP, ICMP and ICMPv6, and was deliberately not extended: a checksum written by a layer whose pseudo-header rules were not tested is worse than one left as it arrived | Construction emits the bytes you set and nothing else, so `bytes(IP()/SCTP())` carries `chksum == 0`. Dissection and round-trip are unaffected — this is exactly why every new layer round-trips byte-identically | OPEN |
| P9 | **Port dispatch is a heuristic, and every application layer that could be confused with a mid-stream segment carries a guard over the payload.** HTTP needs a method token or a status line, TLS a content type of 20..=24 with a 0x03xx version, BGP an all-ones marker, SMB2 its `\xfeSMB`, Modbus a zero protocol id, LDAP a BER SEQUENCE whose length fits, QUIC its fixed bit, SSH a banner or a plausible packet length, MQTT a valid type and remaining length, FTP/SMTP a reply code or an alphabetic verb, IMAP a tag or `*`/`+`. NetFlow v5, v9 and IPFIX share UDP 2055 and are told apart by their version word | Telnet on port 23 has no guard, because its stream has no start-of-message marker to test. A continuation segment on any guarded port falls back to `Raw`, which is the safe direction; a message that starts mid-capture is missed rather than mis-dissected. Dissection is still per packet: nothing here reassembles a TCP stream, so a message split across segments is only seen if its first segment carries the start | PARTIAL |
| P10 | **802.11 is the MAC header and the six management bodies, nothing more.** Control frames (RFC-equivalent IEEE 802.11-2020 §9.3.1) dissect to `Raw` past the MAC header; the FCS trailer is not stripped, so on a capture that includes it the last four octets land in the payload; QoS Control is counted in the header length but not exposed as a field; WEP/CCMP-protected bodies stay `Raw` | The information elements of the six management bodies *are* parsed, as a named item list rather than as scapy's repeating `Dot11Elt` layers | PARTIAL |
| P11 | `DHCP6`'s raw `options` field is declared at octet 4, which is right for a client/server message and wrong for a Relay-forward or Relay-reply, whose fixed part is 34 octets | The *parsed* list (`pkt[DHCP6].options`) is correct for both, because `parse_options` computes the offset from the message type; only `raw_options()` is wrong for a relay message. A conditional variable-length field would fix it and the model has no such thing (E1) | OPEN |
| P12 | **Some layer names and groupings differ from scapy's, deliberately.** mDNS and LLMNR dissect as `DNS` on ports 5353 and 5355, because RFC 6762 §18 and RFC 4795 §2 *are* the RFC 1035 message format and a second copy of the DNS record parser would be a second thing to get wrong. IGMP v1, v2 and v3 are one layer with conditional fields rather than scapy's `IGMP`/`IGMPv3`/`IGMPv3mq`/`IGMPv3mr`, because they share a prefix and differ only in what follows it. An MLDv2 Query is `ICMPv6MLQuery` with four extra conditional fields rather than a separate layer. There is no `Dot11Elt` layer | A script written against those scapy names needs changing. The field names inside each layer do follow scapy's | OPEN |
| P13 | The five Neighbor Discovery and four MLD layers are *whole ICMPv6 messages* — type, code and checksum included — reached from IPv6's Next Header rather than stacked under the generic `ICMPv6` layer, which is how scapy models them, so a chain reads `IPv6/ICMPv6ND_NS` and not `IPv6/ICMPv6/ICMPv6ND_NS` | An ICMPv6 type with no layer of its own still reaches the generic `ICMPv6` | CLOSED |
| T1 | **Text-line protocols do not get a third shape in the engine.** HTTP, SIP, FTP, SMTP, IMAP, Telnet and syslog are CRLF-delimited text with no fixed offsets, so a `FieldDesc` table cannot describe them. They are served instead as the **named item list `options.rs` already produces for a TLV region** — the same `Vec<Item>` through the same `ProtoDesc::parse_options` hook, the same `Cell::Opts` bulk path, the same Python value. `layers/text.rs` is the producer: line splitting, header-block end detection, RFC 9112 §5.2 obs-fold joining, RFC 9110 §5.1 name canonicalisation, start lines and reply codes. The only engine change was to let a layer name the field its item list answers under (`proto::parsed_field_name`), so `pkt[HTTP].headers` and `pkt[FTP].lines` read the way `pkt[TCP].options` does | Remaining: an item list is flat, so a repeated header (`Set-Cookie` twice) appears twice rather than as one list, and `dict(pkt[HTTP].headers)` keeps the last. Header *values* are strings, never parsed further. The body is `Raw`, and a message spanning segments is not reassembled (P9). Syslog's RFC 5424 structured data is split on spaces, so structured data containing a space is cut short | PARTIAL |
| T2 | **BER/ASN.1 gets a reader, not a compiler.** SNMP (RFC 1157 §4.1, RFC 3416 §3) and LDAP (RFC 4511 §4.1.1) have no fixed offsets *at all* — every field's position depends on the length of the one before it — so they, too, are a named item list rather than a field table. `layers/ber.rs` is 120 lines of X.690 §8: identifier and length octets, INTEGER, OBJECT IDENTIFIER, and a bounded walk over a constructed value. SNMP decodes version, community, PDU type, request id, error status and index, and every varbind as `oid -> value`; LDAP decodes messageID, the protocolOp name and its first arguments | Remaining: this is one level of structure at a time, not a schema. Indefinite lengths and high-tag-number forms are refused rather than guessed (neither protocol uses them). SNMPv3 (RFC 3412 §6) has an entirely different message shape and is not decoded — a v3 message yields its outer fields and nothing useful. LDAP search filters (RFC 4511 §4.5.1) are not rendered. No BER *encoder*: these two layers dissect and round-trip, and cannot be constructed field by field | PARTIAL |
| T3 | **A malformed BER message raises nothing; scapy raises `BER_Exception`.** This is a behavioural divergence, not a missing name. CLAUDE.md §5 makes robustness the rule, so an indefinite length, a high-tag-number identifier, a length octet count above four or a length running past the buffer all end the walk and yield whatever was already decoded — an empty item list for a message that is bad from its first octet. Code that relies on catching `BER_Exception` to tell a bad message from a good one has no equivalent here and must test the decoded result instead. `dev/scapy_suite.py` lists `BER_Exception` in `OUT_OF_SCOPE` for that reason, and this row is the honest statement of what that entry hides | Remaining: there is no "was this well-formed?" signal of any kind. A caller that needs one has to compare `raw_options()` against what the item list accounts for | OPEN |
| T4 | **The parsed item lists are capped, and a message past the cap is silently truncated.** `text::lines` stops at 256 lines and `ber::children` at 256 elements per constructed value, which is what keeps a crafted message bounded. An HTTP message with 300 field lines therefore yields the first 256 and no indication that more existed; the same holds for an SNMP PDU with more than 256 varbinds. Measured: 709 KB of input carrying 65,536 distinct headers yields 259 items, in 0.46 ms | Remaining: the cap is a constant, not a budget, so it bounds *count* and not bytes. Byte cost stays linear in the input — a single 100 MB header line decodes in 421 ms and one owned copy, and the worst BER amplification measured is ~7x on invalid UTF-8 (each octet becoming U+FFFD), constant-factor and not compounding. Neither needs DNS's owned-byte budget, and neither reports what it dropped | PARTIAL |
| G1 | `dev/protogen/` generates the layer files and the registration edits from one TOML spec per protocol, and refuses rather than guessing: a missing citation, a duplicate id or name, a field past a fixed header, two fields sharing octets with no condition, a variable-length field that is not last. The citation is a **required key**, which turns the provenance rule from a convention into a build error. Hand-written hooks live in a marked region the generator reads back and preserves. `tests/test_protogen.py` asserts the tree is byte-identical to a regeneration | Remaining: it is a dev tool and not shipped. It cannot express a repeating group (P3), a conditional variable-length field (P11) or a computed field (P8). `[[next.arms]]` generates equality dispatch only. The `[vector]` block seeds a test module once and is not consulted again, so a spec whose vector changes needs its test region cleared | PARTIAL |

## Correctness debt

| # | Item | Status |
|---|---|---|
| C1 | Every derived field of every layer wiry implements is recomputed: the IPv4, TCP, UDP, ICMP and ICMPv6 checksums, the IPv4, IPv6 and UDP lengths, and DNS's RFC 1035 §4.2.2 stream length prefix. No other in-scope layer carries one — Ether, Loopback, CookedLinux, Dot1Q, ARP, BOOTP/DHCP, Raw and Padding have neither a checksum nor a length over their own content — and a unit test walks every built-in field table and fails if a derived field appears with nothing to fill it in | Remaining: a transport checksum needs an enclosing IP layer for its pseudo-header and is left as it was without one, and on a snaplen-clipped capture the TCP, ICMP and ICMPv6 checksums and every length are left as the wire gave them, since nothing derived from the missing bytes can be recomputed. A value the caller assigns by hand is pinned and never recomputed over. A frame the stream splice synthesises is not a captured frame, so its TCP checksum is zeroed rather than left or recomputed — that is E25’s call, not this one’s | CLOSED |
| C2 | Fuzzing: 10 libFuzzer targets in `fuzz/` (Ethernet dissection, any-link dissection, DNS records, pcap, pcapng, capture writing, TLV options, serialise round-trip, fragment reassembly, TCP stream reassembly) plus seeded property tests in `crates/wiry-core/tests/robustness.rs` that run on stable. Both halves are covered: the DNS target now also frames its input as DNS over TCP and runs the whole write pass over it, and the stream target splices every reassembled message back into a frame and dissects the result, and the options target walks the RFC 3046 sub-option table, re-encodes what it walked, checks the raw and decoded walks agree item for item, and drives the RFC 2131 overload and RFC 3396 joining paths, which only a BOOTP header in front reaches | Remaining: no continuous fuzzing, and no coverage-guided corpus beyond the hand-written seeds, which `.gitignore` now keeps distinct from libFuzzer's own output by the forty-hex-digit names it writes. `fuzz/corpus/reassemble/` has no hand-built seed at all, which is the trace left by that target having not compiled for some time | PARTIAL |
| C3 | Endianness assumed little-endian host; big-endian hosts untested. DLT_NULL's address-family field (`layers/null.rs`) is no longer declared `LeUint` unconditionally: `type` is declared twice under disjoint conditions, little-endian where that order names a known address family and big-endian otherwise, so the field read reaches the same decision the dispatcher does and DLT_LOOP (always network order) reports the right value. What remains ambiguous is a DLT_NULL capture written on a big-endian host, which is indistinguishable from DLT_LOOP | PARTIAL |
