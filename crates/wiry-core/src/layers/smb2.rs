//! Microsoft Open Specifications, [MS-SMB2] §2.2.1.2: the SMB2 packet header is a
//! four-octet ProtocolId (0xFE 'S' 'M' 'B'), a 16-bit StructureSize of 64, a
//! 16-bit CreditCharge, a 32-bit Status, a 16-bit Command, a 16-bit
//! CreditRequest/CreditResponse, a 32-bit Flags, a 32-bit NextCommand, a 64-bit
//! MessageId, a 32-bit Reserved, a 32-bit TreeId, a 64-bit SessionId and a
//! 16-octet Signature. Every field but ProtocolId is little-endian.
//!
//! Generated by dev/protogen from dev/protogen/specs/smb2.toml. Field layouts
//! come from the RFC cited above; edit the spec, not this file.

use crate::field::FieldDesc;
use crate::proto::{ProtoDesc, ProtoId};

// protogen:hand begin
/// [MS-SMB2] §2.2.1.2: the four-octet ProtocolId.
pub fn looks_like(p: &[u8]) -> bool {
    p.starts_with(&[0xfe, b'S', b'M', b'B'])
}
// protogen:hand end

pub static FIELDS: &[FieldDesc] = &[
    FieldDesc::uint("ProtocolId", 0, 32, 4266872130),
    FieldDesc::le_uint("StructureSize", 32, 16, 64),
    FieldDesc::le_uint("CreditCharge", 48, 16, 0),
    FieldDesc::le_uint("Status", 64, 32, 0),
    FieldDesc::le_uint("Command", 96, 16, 0),
    FieldDesc::le_uint("CreditRequest", 112, 16, 0),
    FieldDesc::le_uint("Flags", 128, 32, 0),
    FieldDesc::le_uint("NextCommand", 160, 32, 0),
    FieldDesc::le_uint("MessageId", 192, 64, 0),
    FieldDesc::le_uint("Reserved", 256, 32, 0),
    FieldDesc::le_uint("TreeId", 288, 32, 0),
    FieldDesc::le_uint("SessionId", 320, 64, 0),
    FieldDesc::bytes("Signature", 384, 128),
];

fn header_len(_: &[u8]) -> usize {
    64
}

pub static DESC: ProtoDesc = ProtoDesc {
    id: ProtoId::Smb2,
    name: "SMB2_Header",
    fields: FIELDS,
    min_len: 64,
    header_len,
    next: crate::proto::next_raw,
    build_len: 64,
    parse_options: None,
    opt_table: None,
    set_hlen: None,
    bind_next: None,
    bind_next_bytes: None,
    content_len: None,
};

#[cfg(test)]
mod tests {
    #![allow(unused_imports)]
    use super::*;
    use crate::field::FieldValue;
    use crate::packet::Packet;

    // protogen:tests begin
    /// [MS-SMB2] §2.2.3 NEGOTIATE request header: command 0, message id 0, structure size 64.
    fn vector() -> Vec<u8> {
        vec![
            0xfe, 0x53, 0x4d, 0x42, 0x40, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
            0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
        ]
    }

    #[test]
    fn dissects_the_rfc_vector() {
        let p = Packet::dissect(vector(), ProtoId::Smb2);
        let l = p.find_layer(ProtoId::Smb2).unwrap();
        assert_eq!(
            p.get(l, "ProtocolId").unwrap(),
            FieldValue::Uint(4266872130)
        );
        assert_eq!(p.get(l, "StructureSize").unwrap(), FieldValue::Uint(64));
        assert_eq!(p.get(l, "CreditCharge").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "Status").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "Command").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "CreditRequest").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "Flags").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "NextCommand").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "MessageId").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "Reserved").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "TreeId").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.get(l, "SessionId").unwrap(), FieldValue::Uint(0));
        assert_eq!(p.raw_bytes(), &vector()[..]);
    }

    #[test]
    fn truncation_does_not_panic() {
        for n in 0..vector().len() {
            let p = Packet::dissect(vector()[..n].to_vec(), ProtoId::Smb2);
            assert_eq!(p.raw_bytes(), &vector()[..n]);
        }
    }

    #[test]
    fn writes_survive_a_rebuild() {
        let mut p = Packet::dissect(vector(), ProtoId::Smb2);
        let l = p.find_layer(ProtoId::Smb2).unwrap();
        let n = p.raw_bytes().len();
        for f in crate::proto::active_fields(ProtoId::Smb2, false, p.header(l))
            .filter(|f| f.kind == crate::field::FieldKind::Uint && f.bit_len <= 32)
            .map(|f| f.name)
            .collect::<Vec<_>>()
        {
            p.set_uint(l, f, 1);
        }
        assert_eq!(p.to_bytes().len(), n);
    }
    // protogen:tests end
}
