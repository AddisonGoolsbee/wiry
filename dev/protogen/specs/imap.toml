name = "IMAP"
id = "Imap"
num = 91
parsed_field = "lines"
citation = """
RFC 3501 §2.2.1: a client command is a tag, a space, a command and its
arguments, terminated by CR LF. §2.2.2: a server response is either tagged with
the command's tag, untagged and beginning with "*", or a continuation request
beginning with "+". Line-oriented (DEVIATIONS T1).
"""
min_len = 4
header_len = "rest"
next = "end"
parse_options = "hand"

[[fields]]
name = "lines"
off = 0
kind = "var_bytes_to_end"

[[parents]]
from = "tcp_port"
values = [143]
guard = "crate::layers::imap::looks_like"

[vector]
source = "RFC 3501 §7.1.1 untagged greeting."
bytes = [
  0x2a, 0x20, 0x4f, 0x4b, 0x20, 0x49, 0x4d, 0x41,
  0x50, 0x34, 0x72, 0x65, 0x76, 0x31, 0x0d, 0x0a,
]
