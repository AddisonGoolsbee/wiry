name = "ICMPv6MLReport2"
id = "Icmpv6MlReport2"
num = 56
module = "mld_report2"
citation = """
RFC 3810 §5.2, Version 2 Multicast Listener Report: ICMPv6 type 143, a reserved
octet, the checksum, a 16-bit reserved field and a 16-bit count of multicast
address records. §5.2.4 gives one record a one-octet record type, a one-octet
auxiliary data length, a 16-bit source count, the 128-bit multicast address,
that many 128-bit source addresses and then the auxiliary data — so a record
is twenty octets plus sixteen per source plus the auxiliary data, and the
sources are a nested group of their own
(`crates/wiry-core/src/repeat.rs`).

**§5.2.6 counts the auxiliary data in units of 32-bit words**, which is what
the element width uses here. scapy 2.7.0's `ICMPv6MLDMultAddrRec` declares
`auxdata_len` with `length_of="auxdata"` and no adjustment, so it reads the
field as a count of octets and mis-sizes any record that carries auxiliary
data; the RFC is followed instead. The auxiliary data itself is not a named
field: it sits after the nested source list, where the flat record table cannot
place it (DEVIATIONS E1).
"""
min_len = 8
header_len = "rest"
next = "end"
parsed_field = "records"

[[fields]]
name = "type"
off = 0
len = 8
default = 143

[[fields]]
name = "res"
off = 8
len = 8

[[fields]]
name = "cksum"
off = 16
len = 16

[[fields]]
name = "reserved"
off = 32
len = 16

[[fields]]
name = "records_number"
off = 48
len = 16

[[fields]]
name = "records"
off = 64
kind = "var_bytes"

[group]
name = "ICMPv6MLDMultAddrRec"
start = 8
extent = "count"
count_off = 48
count_len = 16
elem_base = 20

[[group.terms]]
off = 16
len = 16
scale = 16

[[group.terms]]
off = 8
len = 8
scale = 4

[[group.fields]]
name = "rtype"
off = 0
len = 8
default = 4

[[group.fields]]
name = "auxdata_len"
off = 8
len = 8

[[group.fields]]
name = "sources_number"
off = 16
len = 16

[[group.fields]]
name = "dst"
off = 32
kind = "ipv6"

[group.nested]
name = "sources"
start = 20
extent = "count"
count_off = 16
count_len = 16
elem_len = 16

[[group.nested.fields]]
name = "src"
off = 0
kind = "ipv6"

[[parents]]
from = "icmpv6_type"
values = [143]

[vector]
source = "RFC 3810 §5.2 report carrying one CHANGE_TO_EXCLUDE record for ff02::1:ff00:2, no sources."
bytes = [
  0x8f, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x01,
  0x04, 0x00, 0x00, 0x00,
  0xff, 0x02, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x01, 0xff, 0x00, 0x00, 0x02,
]
[vector.fields]
type = 143
res = 0
cksum = 0
reserved = 0
records_number = 1

[provenance]
source = "scapy/layers/inet6.py"
version = "scapy 2.7.0"
changed = ["2026-09-18 — ICMPv6MLReport2's field names and ICMPv6MLDMultAddrRec's field table transcribed as a nested repeating group; the auxiliary data length is read in 32-bit words per RFC 3810 §5.2.6, which scapy reads as octets, and it is counted into the record width"]
